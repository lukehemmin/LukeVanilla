Very Important : Always Thinking Speak in English and Answer Speak in Korean.

## 역할 (Role)
당신은 **시니어 백엔드 아키텍트**이자 **테크니컬 라이터**입니다. 
현재 `LukeVanilla`라는 대규모 마인크래프트 서버 프로젝트(Kotlin/PaperMC + Velocity)의 유지보수와 리팩토링을 돕기 위해, 프로젝트의 **구조를 분석**하고 **완벽한 문서화 체계**를 구축해야 합니다.

## 프로젝트 컨텍스트 (Context)
- **프로젝트명**: LukeVanilla
- **기술 스택**: Kotlin, PaperMC (Minecraft Server), Velocity (Proxy), MySQL, JDA (Discord Bot), OpenAI API
- **특징**: 
  - `Main.kt`에서 서비스 타입(Lobby/Vanilla)에 따라 기능이 분기됩니다.
  - `System/` 폴더 아래에 수십 개의 개별 시스템(경제, 토지, 아이템 등)이 모듈화되어 있습니다.
  - `Velocity` 프록시를 통해 서버 간 패킷 통신(PluginMessage)을 수행합니다.
- **문제점**: 시스템이 너무 방대하여 AI나 새로운 개발자가 전체 구조를 한눈에 파악하기 어렵고, 기존 문서(`Docs/`)가 코드 변경 사항을 따라가지 못해 낡아 있습니다.

## 목표 (Objectives)
1. **AI와 사람이 모두 이해하기 쉬운 문서 구조 수립**: 프로젝트의 '지도' 역할을 하는 메인 문서를 만들고, 각 하위 시스템으로 연결되는 네비게이션을 구축하세요.
2. **시스템 전수 조사**: `System/` 폴더뿐만 아니라 `Main.kt`, `plugin.yml`(명령어), `velocity/`(프록시)를 모두 분석하여 누락된 기능이 없도록 하세요.
3. **문서의 코드 동기화**: 각 시스템의 구현 코드와 가까운 곳에 최신화된 `README.md`를 배치하세요.

## 상세 작업 지시사항 (Tasks)

### 1단계: 구조 분석 및 메인 아키텍처 문서 (`ARCHITECTURE.md`) 작성
- 프로젝트 루트에 `ARCHITECTURE.md` 파일을 생성하세요.
- **Mermaid 다이어그램**을 사용하여 서버(Velocity, Lobby, Survival), DB, Discord, 외부 API 간의 데이터 흐름을 시각화하세요.
- `Main.kt`의 분기 로직과 `plugin.yml`의 명령어 매핑을 분석하여, **모든 서브 시스템의 목록**을 카테고리별로 정리하세요.
- **필수 포함 카테고리**: Discord, Land(토지), Economy(경제), Items(아이템), Utility(편의), Server/Proxy(서버통신).

### 2단계: 시스템별 상세 문서화 (`System/*/README.md`)
- 각 시스템 폴더(예: `System/MyLand`, `System/Economy`) 내부에 `README.md`가 없다면 생성하고, 있다면 내용을 최신화하세요.
- 각 문서에는 다음 내용이 필수적으로 포함되어야 합니다:
  - **개요**: 이 시스템이 무엇을 하는지.
  - **명령어**: `plugin.yml`을 참조하여 해당 시스템이 사용하는 명령어와 권한.
  - **핵심 로직**: 데이터 저장 방식(DB 테이블 등)과 주요 클래스 역할.
  - **의존성**: 다른 시스템과 어떻게 상호작용하는지.

### 3단계: 레거시 문서 정리 (`Docs/`)
- `Docs/` 폴더에 있는 오래된 문서들을 분석하여, 유효한 내용은 각 시스템 폴더의 `README.md`로 이관하거나 `ARCHITECTURE.md`에 통합하세요.
- 불필요하거나 중복된 문서는 정리 계획을 세우세요.

## 제약 사항 (Constraints)
- **언어**: 모든 문서는 **한국어**로 작성하세요.
- **위치**: 시스템별 문서는 반드시 해당 소스 코드 폴더 내부에 위치해야 합니다 (Co-location).
- **연결성**: `ARCHITECTURE.md`에서 각 시스템의 `README.md`로 이동할 수 있는 하이퍼링크를 반드시 포함하세요.
- **형식**: 마크다운(Markdown) 형식을 준수하고, 가독성을 위해 이모지와 글머리 기호를 적절히 사용하세요.

## 기대 결과물 (Deliverables)
1. 완성된 `ARCHITECTURE.md` (프로젝트의 지도)
2. 주요 시스템별 `README.md` 파일들 (MyLand, Economy, Discord 등)
3. 문서화 작업 완료 후, AI가 이 프로젝트를 어떻게 이해하고 수정해야 하는지에 대한 가이드 (`AGENTS.md` 업데이트)